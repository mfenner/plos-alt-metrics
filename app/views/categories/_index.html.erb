<div class="content">
  <div class="page-header">
    <h2>Categories <small>Categories are a way to organize sources.</small> <%= link_to image_tag("add.gif"), new_category_path %></h2>
  </div>

	<ul class="unstyled">
	<% for category in @categories %>
	  <li><h3><%= link_to h(category.name), category %></h3></li>
	<% end %>
	</ul>
</div>