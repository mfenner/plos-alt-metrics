<% if params[:partial] == "twitter" %>
  $('#twitter').html("<%= escape_javascript(render "twitter") %>");
<% else %>
  $('#service').html("<%= escape_javascript(render "service") %>");
<% end %>

$(function () {
  $('.tabs').tabs()
})