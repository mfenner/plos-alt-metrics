<% if params[:service] == "scopus" and ((controller.action_name == "edit" or (controller.action_name == "update" and !@author.errors.empty?))) %>
  <div class="edit">
	  <%= form_for :author, :url => author_path(@author.username, :partial => "service", :service => "scopus"), :remote => true, :method => :put do |f| %>
    <h3>Scopus Author ID</h3>
		    <%= f.text_field :scopus %>
		  <p>
			  <%= link_to "Cancel", author_path(@author.username, :partial => "service"), :remote => true, :method => :get %>
		    <%= f.submit("Submit") %>
		    <%= image_tag 'spinner.gif', :id=>'spinner', :style=>"display:none;" %>
		<% end %>
	</div>
<% else %>
	  <%= image_tag "scopus.png", :class => "photo_mini", :title => "Scopus" %><br/>
		<span class="name"><%= @author.scopus.blank? ? "not set" : link_to(@author.scopus, "http://scopus.org/profile/" + @author.scopus) %></span>
		<% if current_author == @author or current_author.try(:admin?) %>&nbsp;<%= link_to image_tag("edit.gif"), edit_author_path(@author.username, :partial => "service", :service => "scopus"),
		                                       :remote => true,
		  																		 :method => :get },
		                                       :title => "Edit Scopus" %>
		<% end %>
<% end %>