<% unless (controller.action_name == "edit" or (controller.action_name == "update" and !@author.errors.empty?)) and ["mendeley", "mas"].include?(params[:service]) %>
	<div class="service">
	  <%= image_tag "twitter.png", :class => "photo_mini" %><br/>
	  <span class="name"><%= link_to "@#{@author.username}", "http://twitter.com/" + @author.username %></span>
	</div>
<% end %>

<% if !@author.mendeley.blank? or current_author == @author or current_author.try(:admin?) %>
	<% unless (controller.action_name == "edit" or (controller.action_name == "update" and !@author.errors.empty?)) and ["mas"].include?(params[:service]) %>
	  <div id="mendeley">
	    <%= render :partial => 'mendeley' %>
	  </div>
	<% end %>
<% end %>
<% if !@author.mas.blank? or current_author == @author or current_author.try(:admin?) %>
  <% unless (controller.action_name == "edit" or (controller.action_name == "update" and !@author.errors.empty?)) and ["mendeley"].include?(params[:service]) %>
	  <div id="mas">
	    <%= render :partial => 'mas' %>
	  </div>
	<% end %>
<% end %>